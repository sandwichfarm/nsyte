---
id: 23-01
phase: 23-homebrew-tap-pipeline
title: "Replace publish-homebrew stub with real Homebrew tap publish pipeline"
wave: 1
status: pending
goal: >
  The publish-homebrew CI job clones sandwichfarm/homebrew-nsyte, patches
  packages/homebrew/Formula/nsyte.rb with version and SHA256 values from the
  setup job, commits, and pushes to the tap repo via HOMEBREW_TAP_TOKEN PAT.
  docs/RELEASING.md documents the secret setup and bootstrap steps.
requirements: [BREW-02, BREW-03, BREW-04]
modified_files:
  - .github/workflows/publish-packages.yml
  - docs/RELEASING.md
autonomous: true
depends_on: []
files_modified:
  - .github/workflows/publish-packages.yml
  - docs/RELEASING.md

must_haves:
  truths:
    - "The publish-homebrew job clones the tap repo, patches Formula/nsyte.rb with real values, commits, and pushes on release"
    - "All four PLACEHOLDER_ markers in the formula are replaced before any commit is made; the job fails fast if any survive"
    - "The HOMEBREW_TAP_TOKEN secret is only injected via env: — never via ${{ }} inside a run: block"
    - "The job is idempotent: re-running on the same version skips the push when nothing changed"
    - "docs/RELEASING.md documents how to create the PAT, add the secret, bootstrap the tap repo, and verify the formula locally"
  artifacts:
    - path: ".github/workflows/publish-packages.yml"
      provides: "Real publish-homebrew job replacing the stub"
      contains: "HOMEBREW_TAP_TOKEN"
    - path: "docs/RELEASING.md"
      provides: "HOMEBREW_TAP_TOKEN prerequisite section + bootstrap + local verification recipe"
      contains: "HOMEBREW_TAP_TOKEN prerequisite"
  key_links:
    - from: ".github/workflows/publish-packages.yml publish-homebrew job"
      to: "needs.setup.outputs (version, sha256_macos_arm64, sha256_macos_x64, sha256_linux_x64)"
      via: "env: blocks on each step that needs a value"
      pattern: "needs\\.setup\\.outputs\\."
    - from: "publish-homebrew patch step"
      to: "/tmp/tap/Formula/nsyte.rb"
      via: "four sed -i substitutions"
      pattern: "sed -i.*PLACEHOLDER_"
    - from: "publish-homebrew push step"
      to: "sandwichfarm/homebrew-nsyte main"
      via: "HTTPS PAT embedded in clone URL; git push origin main"
      pattern: "git.*push origin main"
---

<objective>
Replace the three-line TODO stub in the `publish-homebrew` job with a real clone-patch-push
pipeline that mirrors the working `publish-aur` job structure.

Purpose: BREW-02 (auto-update formula on release), BREW-03 (PAT push succeeds), BREW-04
(local verification recipe documented).

Output:
- `.github/workflows/publish-packages.yml` — `publish-homebrew` job with 4 steps: checkout
  nsyte, clone tap, patch formula, commit and push.
- `docs/RELEASING.md` — three new sections appended after the AUR sections: PAT prerequisite,
  tap bootstrap (one-time), and local formula verification recipe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. No exploration needed. -->

From .github/workflows/publish-packages.yml — setup job outputs (already declared):
```yaml
outputs:
  version:           ${{ steps.extract.outputs.version }}
  sha256_linux_x64:  ${{ steps.checksums.outputs.sha256_linux_x64 }}
  sha256_macos_arm64: ${{ steps.checksums.outputs.sha256_macos_arm64 }}
  sha256_macos_x64:  ${{ steps.checksums.outputs.sha256_macos_x64 }}
```

From packages/homebrew/Formula/nsyte.rb — exact placeholder strings:
```
PLACEHOLDER_VERSION            (line 5 — version field)
PLACEHOLDER_SHA256_MACOS_ARM64 (line 11 — on_macos/on_arm sha256)
PLACEHOLDER_SHA256_MACOS_X64   (line 15 — on_macos/on_intel sha256)
PLACEHOLDER_SHA256_LINUX_X86_64 (line 21 — on_linux/on_intel sha256)
```
Note: The URL lines use Ruby #{version} interpolation — NOT a placeholder; do not touch them.

From .github/workflows/publish-packages.yml — publish-aur job structure to mirror:
- uses: actions/checkout@v4  (checkout nsyte repo for template access)
- git clone <remote> /tmp/<dir>
- env: VERSION / SHA256_* → cp template → sed -i substitutions → grep -q PLACEHOLDER_ guard
- git -C /tmp/<dir> config user.name / user.email
- git -C /tmp/<dir> add → git diff --cached --quiet guard → commit → push

From docs/RELEASING.md — insertion point for new sections:
- Existing last section ends at line 212 ("Related files")
- New sections go BEFORE "## Related files" (append before that heading)
- Existing AUR section structure to mirror:
  "## AUR_SSH_PRIVATE_KEY prerequisite" → why required + symptom
  "## Generating and registering..." → step-by-step setup
  "## Verifying the AUR PKGBUILD locally (AUR-05)" → local recipe
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace publish-homebrew stub in publish-packages.yml</name>
  <files>.github/workflows/publish-packages.yml</files>
  <action>
Replace the entire `publish-homebrew` job block (lines 172-183) with the following real
implementation. The replacement must preserve the surrounding jobs exactly — do not touch
`publish-scoop`, `publish-winget`, or `publish-nix` stubs.

The replacement job:

```yaml
  publish-homebrew:
    name: Publish to Homebrew
    needs: [setup]
    if: needs.setup.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout nsyte repo (for formula template)
        uses: actions/checkout@v4

      - name: Clone Homebrew tap repo
        env:
          HOMEBREW_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
        run: |
          git clone "https://x-access-token:${HOMEBREW_TAP_TOKEN}@github.com/sandwichfarm/homebrew-nsyte.git" /tmp/tap

      - name: Patch formula from template
        env:
          VERSION: ${{ needs.setup.outputs.version }}
          SHA256_MACOS_ARM64: ${{ needs.setup.outputs.sha256_macos_arm64 }}
          SHA256_MACOS_X64: ${{ needs.setup.outputs.sha256_macos_x64 }}
          SHA256_LINUX_X64: ${{ needs.setup.outputs.sha256_linux_x64 }}
        run: |
          mkdir -p /tmp/tap/Formula
          cp packages/homebrew/Formula/nsyte.rb /tmp/tap/Formula/nsyte.rb
          sed -i "s|PLACEHOLDER_VERSION|${VERSION}|g"                       /tmp/tap/Formula/nsyte.rb
          sed -i "s|PLACEHOLDER_SHA256_MACOS_ARM64|${SHA256_MACOS_ARM64}|g" /tmp/tap/Formula/nsyte.rb
          sed -i "s|PLACEHOLDER_SHA256_MACOS_X64|${SHA256_MACOS_X64}|g"     /tmp/tap/Formula/nsyte.rb
          sed -i "s|PLACEHOLDER_SHA256_LINUX_X86_64|${SHA256_LINUX_X64}|g"  /tmp/tap/Formula/nsyte.rb
          # Sanity: fail fast if any placeholder survived substitution
          if grep -q "PLACEHOLDER_" /tmp/tap/Formula/nsyte.rb; then
            echo "::error::Formula still contains PLACEHOLDER_ markers after substitution."
            grep "PLACEHOLDER_" /tmp/tap/Formula/nsyte.rb
            exit 1
          fi

      - name: Commit and push to tap repo
        env:
          VERSION: ${{ needs.setup.outputs.version }}
        run: |
          git -C /tmp/tap config user.name "nsyte-bot"
          git -C /tmp/tap config user.email "nsyte-bot@users.noreply.github.com"
          git -C /tmp/tap add Formula/nsyte.rb
          if git -C /tmp/tap diff --cached --quiet; then
            echo "No changes to Formula/nsyte.rb — tap already up to date for v${VERSION}. Skipping push."
            exit 0
          fi
          git -C /tmp/tap commit -m "nsyte ${VERSION}"
          git -C /tmp/tap push origin main
```

Implementation notes:
- `timeout-minutes: 10` is added (absent in the stub, present in publish-aur — per D match).
- The clone step puts the token in `env:` — NEVER inline as `${{ secrets.X }}` inside `run:`.
- `mkdir -p /tmp/tap/Formula` must precede `cp` to handle empty tap repos (no Formula/ dir).
- The `sed` delimiter is `|` not `/` — SHA256 hex values do not contain `|` but they do
  contain alphanumerics only; `|` avoids any accidental `/` collision from the URL lines
  (those use `#{version}` Ruby interpolation which is untouched).
- The placeholder `PLACEHOLDER_SHA256_LINUX_X86_64` maps to setup output `sha256_linux_x64`
  (note: output key omits `_x86` prefix — this mapping is intentional and verified).
- Idempotency: `git diff --cached --quiet` exits 0 when staged tree is clean (no changes),
  so we skip commit+push on re-runs for the same version.
- Commit message is `nsyte ${VERSION}` (no `v` prefix, no `chore:` prefix — Homebrew
  convention per locked decision).
- Push target is `origin main` — tap repo was created with `main` as default branch.
  </action>
  <verify>
    <automated>
      # Confirm stub is gone
      grep -v '^#' .github/workflows/publish-packages.yml | grep -c 'TODO — phase 23' || true
      # Above should print 0 (no matches)
      # Confirm key structural elements are present
      grep -c 'HOMEBREW_TAP_TOKEN' .github/workflows/publish-packages.yml
      # Should be >= 2 (env: declaration + secrets reference)
      grep -c 'PLACEHOLDER_SHA256_LINUX_X86_64' .github/workflows/publish-packages.yml
      # Should be 1 (the sed substitution line)
      grep -c 'git.*push origin main' .github/workflows/publish-packages.yml
      # Should be 1
      grep -c 'diff --cached --quiet' .github/workflows/publish-packages.yml
      # Should be 1 (idempotency guard)
    </automated>
  </verify>
  <done>
    - The `publish-homebrew` job no longer contains any TODO or stub text
    - The job has exactly 4 steps: Checkout, Clone tap, Patch formula, Commit and push
    - `HOMEBREW_TAP_TOKEN` appears only in `env:` blocks, never as `${{ secrets.X }}` inside
      a `run:` block
    - All four PLACEHOLDER_ sed substitutions are present using `|` delimiter
    - The post-sed sanity guard (`grep -q "PLACEHOLDER_"`) is present
    - The idempotency guard (`git diff --cached --quiet`) is present before commit
    - Push target is `origin main`
    - The four sibling jobs (publish-scoop, publish-winget, publish-nix) are unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Append HOMEBREW_TAP_TOKEN sections to docs/RELEASING.md</name>
  <files>docs/RELEASING.md</files>
  <action>
Append three new sections to `docs/RELEASING.md` BEFORE the existing `## Related files`
section (currently the last section at the bottom of the file). The content to insert:

---

```markdown
---

## HOMEBREW_TAP_TOKEN prerequisite

A Personal Access Token (PAT) named `HOMEBREW_TAP_TOKEN` **must** be configured as a
repository secret before the `publish-homebrew` CI job can push to the Homebrew tap repo.

**Why this is required:** The Homebrew tap (`sandwichfarm/homebrew-nsyte`) is a separate
GitHub repository. The `publish-homebrew` job clones it, patches `Formula/nsyte.rb` with
the release version and SHA256 hashes, and pushes the commit back. `GITHUB_TOKEN` only has
write access to the repo it runs in (`sandwichfarm/nsyte`), not to the separate tap repo.
A PAT with Contents write permission on `sandwichfarm/homebrew-nsyte` is required.

**Symptom of forgetting the secret:** The `publish-homebrew` job fails at the clone or
push step with `Authentication failed` or `repository not found`. The sibling jobs
(`publish-aur`, `publish-scoop`, etc.) are unaffected — failure isolation is by design
(INFRA-05).

---

## Creating the HOMEBREW_TAP_TOKEN PAT (fine-grained, preferred)

1. Navigate to https://github.com/settings/personal-access-tokens
2. Click **Generate new token**.
3. Set a descriptive name (e.g., `nsyte homebrew tap publish`).
4. Set an expiry date (90 days recommended). Avoid "No expiration".
5. Under **Repository access**, select **Only select repositories** → choose
   `sandwichfarm/homebrew-nsyte`. Do **not** grant access to `sandwichfarm/nsyte` — that
   would violate least privilege.
6. Under **Repository permissions**, set **Contents** to **Read and write**. No other
   permissions are needed.
7. Click **Generate token**. Copy immediately — it starts with `github_pat_`.

---

## Adding HOMEBREW_TAP_TOKEN to repository secrets

1. Navigate to https://github.com/sandwichfarm/nsyte/settings/secrets/actions
2. Click **New repository secret**.
3. Name: `HOMEBREW_TAP_TOKEN` — exactly this, case-sensitive, no whitespace.
4. Value: paste the PAT string from above.
5. Click **Add secret**.

Confirm the secret exists with the GitHub CLI:

```bash
gh secret list -R sandwichfarm/nsyte
```

Both `HOMEBREW_TAP_TOKEN` and `AUR_SSH_PRIVATE_KEY` should appear alongside `RELEASE_TOKEN`.

---

## Bootstrapping the Homebrew tap (one-time)

The `sandwichfarm/homebrew-nsyte` repository must exist before CI can push to it. This is a
one-time manual step.

1. **Create the tap repo** — navigate to https://github.com/new and create a **public** repo
   named `homebrew-nsyte` under the `sandwichfarm` account. Homebrew tap convention requires
   the `homebrew-` prefix. Initialize with a `README.md` so the default branch (`main`)
   exists.
2. **Add `HOMEBREW_TAP_TOKEN` secret** — follow the steps above.
3. **Trigger a release** — the `publish-homebrew` job will clone the repo, create
   `Formula/nsyte.rb`, commit, and push. The first CI push populates the Formula directory.

**Empty-repo note:** `git clone` of a repo that contains only `README.md` succeeds. The
`mkdir -p /tmp/tap/Formula` in the patch step ensures `Formula/` is created even if it
does not exist in the tap repo yet. The first push creates the `Formula/` directory in the
tap.

**If the tap repo does not exist at clone time:** The clone step fails with
`repository not found`. Create the repo first, then re-run the workflow via
`workflow_dispatch`.

---

## Verifying the Homebrew formula locally (BREW-04)

A maintainer can verify the patched formula installs correctly before a real CI run.
This requires Homebrew installed on macOS (or Homebrew on Linux).

```bash
# Pick a published version
VERSION="1.5.0"

# Compute SHA256s for each platform binary
SHA256_MACOS_ARM64=$(curl -fsSL "https://github.com/sandwichfarm/nsyte/releases/download/v${VERSION}/nsyte-macos-arm64-${VERSION}" | sha256sum | awk '{print $1}')
SHA256_MACOS_X64=$(curl -fsSL "https://github.com/sandwichfarm/nsyte/releases/download/v${VERSION}/nsyte-macos-x64-${VERSION}" | sha256sum | awk '{print $1}')
SHA256_LINUX_X64=$(curl -fsSL "https://github.com/sandwichfarm/nsyte/releases/download/v${VERSION}/nsyte-linux-${VERSION}" | sha256sum | awk '{print $1}')

# Patch a working copy (same logic as CI)
mkdir -p /tmp/brew-test/Formula
cp packages/homebrew/Formula/nsyte.rb /tmp/brew-test/Formula/nsyte.rb
sed -i "s|PLACEHOLDER_VERSION|${VERSION}|g"                       /tmp/brew-test/Formula/nsyte.rb
sed -i "s|PLACEHOLDER_SHA256_MACOS_ARM64|${SHA256_MACOS_ARM64}|g" /tmp/brew-test/Formula/nsyte.rb
sed -i "s|PLACEHOLDER_SHA256_MACOS_X64|${SHA256_MACOS_X64}|g"     /tmp/brew-test/Formula/nsyte.rb
sed -i "s|PLACEHOLDER_SHA256_LINUX_X86_64|${SHA256_LINUX_X64}|g"  /tmp/brew-test/Formula/nsyte.rb

# Sanity check: no PLACEHOLDER markers should remain
grep "PLACEHOLDER_" /tmp/brew-test/Formula/nsyte.rb && { echo "substitution failed"; exit 1; }

# On macOS with Homebrew installed — install directly from the patched local formula file
brew install --formula /tmp/brew-test/Formula/nsyte.rb
nsyte --version
```

If `nsyte --version` prints the correct version, the formula is well-formed and the binary
is installable. The same substitution logic runs in CI; a green local run gives confidence
in the upcoming CI push to the tap.

**Alternatively — verify via the tap after a CI push:**

```bash
brew tap sandwichfarm/nsyte https://github.com/sandwichfarm/homebrew-nsyte
brew install nsyte
nsyte --version
```

This is the end-user install path and the final functional check after a real release.
```

---

Insert this content immediately before the `## Related files` line at the bottom of
`docs/RELEASING.md`. The `## Related files` section must remain the final section of the
document — move it to after the newly inserted content if it would otherwise precede it.

After insertion, also add the following bullet to the `## Related files` section:

```
- `packages/homebrew/Formula/nsyte.rb` — the Homebrew formula template. Contains four
  `PLACEHOLDER_` markers that CI substitutes per-release before pushing to the tap. Maintainers
  can substitute these by hand for local verification (see "Verifying the Homebrew formula
  locally" above).
```
  </action>
  <verify>
    <automated>
      grep -c 'HOMEBREW_TAP_TOKEN prerequisite' docs/RELEASING.md
      # Should be 1
      grep -c 'Bootstrapping the Homebrew tap' docs/RELEASING.md
      # Should be 1
      grep -c 'Verifying the Homebrew formula locally' docs/RELEASING.md
      # Should be 1
      # Confirm Related files is still the last H2 heading
      tail -20 docs/RELEASING.md | grep -c 'Related files'
      # Should be 1
    </automated>
  </verify>
  <done>
    - Three new sections are present in docs/RELEASING.md:
      "HOMEBREW_TAP_TOKEN prerequisite", "Bootstrapping the Homebrew tap (one-time)",
      and "Verifying the Homebrew formula locally (BREW-04)"
    - All three sections appear BEFORE the existing "## Related files" section
    - The "## Related files" section remains the last section of the document
    - The Related files section contains a bullet for packages/homebrew/Formula/nsyte.rb
    - The local verification recipe uses `|` sed delimiters matching the CI job
    - The PAT creation steps describe fine-grained token with single-repo scope
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → tap repo | HTTPS PAT authenticates git push; token injected via env: secret |
| workflow YAML → secrets | ${{ secrets.X }} must never appear inside run: blocks — only in env: blocks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Information Disclosure | HOMEBREW_TAP_TOKEN in run: block | mitigate | Token injected only via `env:` block; GitHub Actions masks registered secret values in logs; ${{ secrets.X }} never appears inside a run: block |
| T-23-02 | Information Disclosure | Token embedded in git clone URL (stored in .git/config) | accept | ubuntu-latest runners are ephemeral; workspace destroyed after job; not a concern for GitHub-hosted runners |
| T-23-03 | Tampering | Formula placeholder not fully substituted | mitigate | Post-sed `grep -q "PLACEHOLDER_"` guard fails the job before any commit if any marker survives |
| T-23-04 | Elevation of Privilege | PAT scoped too broadly | mitigate | Fine-grained PAT with Contents: Read+write on sandwichfarm/homebrew-nsyte only; explicitly exclude sandwichfarm/nsyte from PAT scope |
| T-23-05 | Denial of Service | Re-running job on same release pushes duplicate commit | mitigate | `git diff --cached --quiet` idempotency guard skips push when formula is unchanged |
</threat_model>

<verification>
After both tasks are complete:

1. Confirm stub is fully replaced:
   ```bash
   grep -v '^#' .github/workflows/publish-packages.yml | grep 'TODO.*phase 23'
   # Should return no output
   ```

2. Confirm token only appears in env: blocks:
   ```bash
   grep -n 'secrets.HOMEBREW_TAP_TOKEN' .github/workflows/publish-packages.yml
   # All matches should be inside env: stanzas, not inside run: blocks
   ```

3. Confirm all four sed substitutions are present with | delimiter:
   ```bash
   grep 'sed -i.*PLACEHOLDER_' .github/workflows/publish-packages.yml
   # Should show 4 lines
   ```

4. Confirm RELEASING.md has all three new sections:
   ```bash
   grep -n '^## HOMEBREW_TAP_TOKEN\|^## Bootstrapping the Homebrew\|^## Verifying the Homebrew' docs/RELEASING.md
   # Should show 3 matches
   ```

5. Confirm RELEASING.md section order (new sections before Related files):
   ```bash
   grep -n '^## ' docs/RELEASING.md | tail -5
   # "Related files" must be the last H2
   ```

6. YAML syntax check (optional, requires yq or python):
   ```bash
   python3 -c "import yaml; yaml.safe_load(open('.github/workflows/publish-packages.yml'))" && echo "YAML valid"
   ```
</verification>

<success_criteria>
- BREW-02: The `publish-homebrew` job patches all four PLACEHOLDER_ markers in Formula/nsyte.rb
  with values from setup job outputs (version, sha256_macos_arm64, sha256_macos_x64,
  sha256_linux_x64) before committing to the tap
- BREW-03: The job authenticates via HOMEBREW_TAP_TOKEN PAT and pushes to
  sandwichfarm/homebrew-nsyte main; docs/RELEASING.md documents secret creation and
  bootstrap steps
- BREW-04: docs/RELEASING.md contains a local verification recipe that a maintainer can run
  on macOS to confirm the formula is well-formed and the binary installs correctly
- Security: HOMEBREW_TAP_TOKEN is injected only via env: blocks; never appears as
  ${{ secrets.X }} inside a run: block
- Idempotency: job exits 0 without pushing when re-run on an already-published version
</success_criteria>

<output>
After completion, create `.planning/phases/23-homebrew-tap-pipeline/23-01-SUMMARY.md`
using the template at `$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
