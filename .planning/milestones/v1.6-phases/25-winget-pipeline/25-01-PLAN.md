---
id: 25-01
phase: 25-winget-pipeline
plan: 01
title: "Replace publish-winget stub with real wingetcreate update pipeline"
type: execute
wave: 1
status: complete
depends_on: []
autonomous: true
goal: >
  The publish-winget CI job downloads wingetcreate, runs wingetcreate update to submit
  a version-bump PR to microsoft/winget-pkgs, and gracefully skips if the package is not
  yet in the index. docs/RELEASING.md documents the secret setup, one-time bootstrap PR,
  and local verification.
requirements: [WINGET-02, WINGET-03, WINGET-04, WINGET-05]
files_modified:
  - .github/workflows/publish-packages.yml
  - docs/RELEASING.md

must_haves:
  truths:
    - "publish-winget job runs on windows-latest (not ubuntu-latest like the stub)"
    - "WINGET_CREATE_GITHUB_TOKEN is passed via env: block, never via --token flag"
    - "wingetcreate exits non-zero before bootstrap PR merges; job exits 0 with a warning rather than failing"
    - "docs/RELEASING.md documents the classic PAT requirement, creation steps, bootstrap PR procedure, and local manifest verification"
    - "No ${{ }} expressions appear inside run: blocks — all are in env: blocks"
  artifacts:
    - path: ".github/workflows/publish-packages.yml"
      provides: "Real publish-winget job replacing the stub"
      contains: "wingetcreate.exe update sandwichfarm.nsyte"
    - path: "docs/RELEASING.md"
      provides: "WINGET_FORK_TOKEN setup, bootstrap PR, and local verify documentation"
      contains: "WINGET_FORK_TOKEN"
  key_links:
    - from: "publish-winget job"
      to: "needs.setup.outputs.version + sha256_windows"
      via: "env: VERSION / SHA256_WINDOWS in steps"
      pattern: "needs\\.setup\\.outputs\\.version"
    - from: "wingetcreate"
      to: "WINGET_FORK_TOKEN secret"
      via: "WINGET_CREATE_GITHUB_TOKEN env var"
      pattern: "WINGET_CREATE_GITHUB_TOKEN"
---

<objective>
Replace the `publish-winget` stub job in `.github/workflows/publish-packages.yml` with a
real pipeline that uses `wingetcreate update` to submit version-bump PRs to
`microsoft/winget-pkgs` on every release.

Purpose: nsyte releases should automatically appear in the Windows Package Manager index
without manual intervention after the one-time bootstrap PR is merged.

Output:
- Working `publish-winget` job on `windows-latest` using `wingetcreate`
- RELEASING.md sections covering WINGET_FORK_TOKEN, bootstrap PR, and local verification
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

Key facts from research (ManifestVersion 1.12.0 confirmed correct; WR-02 resolved):
- wingetcreate self-contained download: `https://aka.ms/wingetcreate/latest/self-contained`
- Token env var: `WINGET_CREATE_GITHUB_TOKEN` (NOT `--token` flag — logs the value)
- Classic PAT required (fine-grained explicitly not supported by wingetcreate)
- Required scopes: `public_repo` (mandatory), `delete_repo` (optional, for fork cleanup)
- `--no-open` flag required — prevents runner hang on headless CI
- `--urls "...exe|x64"` arch override recommended (filename has no arch substring)
- Non-zero exit before bootstrap = expected; treat all non-zero exits as warn+exit-0
- `wingetcreate update` reads live manifest from winget-pkgs index, NOT local templates
- Local templates in `packages/winget/` are bootstrap reference only, remain unchanged

Existing job structure in publish-packages.yml:
- `setup` job (ubuntu-latest) outputs: version, sha256_linux_x64, sha256_macos_arm64, sha256_macos_x64, sha256_windows
- `publish-winget` job currently: `runs-on: ubuntu-latest`, single stub step exiting 0
- All sibling jobs (publish-aur, publish-homebrew, publish-scoop) use pattern:
  `needs: [setup]`, `if: needs.setup.result == 'success'`

RELEASING.md structure: Ends with `## Related files` section. New sections must be
inserted immediately before that section to follow the existing per-token pattern:
  prerequisite → PAT creation → adding secret → bootstrapping → local verification
</context>

<interfaces>
<!-- Extracted from publish-packages.yml setup job outputs -->
setup job outputs:
  version:         string  (e.g. "1.6.0", stripped of leading "v")
  sha256_linux_x64:   string  (hex SHA256)
  sha256_macos_arm64: string  (hex SHA256)
  sha256_macos_x64:   string  (hex SHA256)
  sha256_windows:     string  (hex SHA256 of nsyte-windows-$VERSION.exe)

Reference to outputs in dependent jobs:
  ${{ needs.setup.outputs.version }}
  ${{ needs.setup.outputs.sha256_windows }}

Existing sibling job pattern (publish-scoop as reference):
  publish-scoop:
    name: Publish to Scoop
    needs: [setup]
    if: needs.setup.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - env: ...
        run: |
          ...

packages/winget/ templates (bootstrap reference, NOT modified by this plan):
  sandwichfarm.nsyte.installer.yaml  — ManifestVersion: "1.12.0", InstallerType: portable
  sandwichfarm.nsyte.locale.en-US.yaml
  sandwichfarm.nsyte.version.yaml
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace publish-winget stub with real wingetcreate job</name>
  <files>.github/workflows/publish-packages.yml</files>
  <action>
Replace the entire `publish-winget` job block (lines 272-286, currently the stub) with
the following. Keep every other job and the workflow-level config untouched.

The replacement job:

```yaml
  publish-winget:
    name: Publish to Winget
    needs: [setup]
    if: needs.setup.result == 'success'
    runs-on: windows-latest
    timeout-minutes: 15
    steps:
      - name: Checkout nsyte repo (for ManifestVersion sanity check)
        uses: actions/checkout@v4

      - name: Verify ManifestVersion in templates (informational, resolves WR-02)
        shell: pwsh
        run: |
          $expected = "1.12.0"
          $match = Select-String -Path "packages\winget\sandwichfarm.nsyte.installer.yaml" `
            -Pattern "ManifestVersion:\s*(.+)"
          $templateMV = $match.Matches[0].Groups[1].Value.Trim()
          if ($templateMV -ne $expected) {
            Write-Host "::warning::Template ManifestVersion ($templateMV) differs from expected ($expected). Update packages/winget/ templates before the next bootstrap."
          } else {
            Write-Host "ManifestVersion verified: $templateMV"
          }

      - name: Download wingetcreate (self-contained, no .NET dependency)
        shell: pwsh
        run: |
          iwr https://aka.ms/wingetcreate/latest/self-contained -OutFile wingetcreate.exe

      - name: Submit manifest PR to winget-pkgs
        env:
          VERSION: ${{ needs.setup.outputs.version }}
          REPO: ${{ github.repository }}
          WINGET_CREATE_GITHUB_TOKEN: ${{ secrets.WINGET_FORK_TOKEN }}
        shell: pwsh
        run: |
          $installerUrl = "https://github.com/$($env:REPO)/releases/download/v$($env:VERSION)/nsyte-windows-$($env:VERSION).exe"
          Write-Host "Submitting winget manifest for version $($env:VERSION)"
          Write-Host "Installer URL: $installerUrl"

          .\wingetcreate.exe update sandwichfarm.nsyte `
            --version $env:VERSION `
            --urls "$installerUrl|x64" `
            --submit `
            --no-open

          $exitCode = $LASTEXITCODE
          if ($exitCode -ne 0) {
            Write-Host "::warning::wingetcreate exited with code $exitCode."
            Write-Host "::warning::If sandwichfarm.nsyte is not yet in the winget-pkgs index, this is expected before the manual bootstrap PR is merged."
            Write-Host "::warning::See docs/RELEASING.md for winget bootstrap instructions."
            exit 0
          }
          Write-Host "Winget manifest PR submitted successfully."
```

Security constraints (enforce these exactly):
- `WINGET_CREATE_GITHUB_TOKEN` is in the `env:` block of the step — this is the env var
  wingetcreate reads natively. Do NOT pass it as `--token $env:WINGET_CREATE_GITHUB_TOKEN`
  or any other CLI flag; that would expose it in logs.
- All `${{ }}` expressions appear exclusively in `env:` blocks, never inside `run:` blocks.
- `$($env:VERSION)` and `$($env:REPO)` are PowerShell env-var expansions inside the
  run script — this is correct and does not violate the above rule.

Verification: The `--no-open` flag prevents the runner from hanging. The `|x64` arch
override in the URL handles wingetcreate's regex-based architecture detection for filenames
that lack an explicit arch substring.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nsyte && python3 -c "
import yaml, sys
with open('.github/workflows/publish-packages.yml') as f:
    wf = yaml.safe_load(f)
job = wf['jobs']['publish-winget']
assert job['runs-on'] == 'windows-latest', 'runs-on must be windows-latest'
steps = job['steps']
names = [s.get('name','') for s in steps]
assert any('wingetcreate' in n.lower() for n in names), 'missing wingetcreate download step'
submit_step = next(s for s in steps if 'Submit' in s.get('name',''))
assert 'WINGET_CREATE_GITHUB_TOKEN' in submit_step.get('env',{}), 'token must be in env block'
assert '--token' not in submit_step.get('run',''), 'must not use --token flag'
run_block = submit_step.get('run','')
assert '\${{' not in run_block and '\${{' not in run_block, 'no expressions in run block'
assert '--no-open' in run_block, 'missing --no-open flag'
assert '|x64' in run_block, 'missing |x64 arch override'
print('PASS: publish-winget job structure correct')
"
</automated>
  </verify>
  <done>
publish-winget job:
- runs-on: windows-latest (not ubuntu-latest)
- has 4 steps: checkout, ManifestVersion verify, download wingetcreate, submit PR
- WINGET_CREATE_GITHUB_TOKEN in env: block of submit step, not as CLI flag
- --no-open present, --submit present, |x64 arch override present
- exit-0 guard for non-zero wingetcreate exit codes
- no ${{ }} expressions inside any run: block
  </done>
</task>

<task type="auto">
  <name>Task 2: Append WINGET_FORK_TOKEN sections to docs/RELEASING.md</name>
  <files>docs/RELEASING.md</files>
  <action>
Insert the following content into `docs/RELEASING.md` immediately BEFORE the existing
`## Related files` section (currently the last section, at line 474). Do not modify
any existing content. Add a blank line before and after the inserted block.

---

## WINGET_FORK_TOKEN prerequisite

A Personal Access Token (PAT) named `WINGET_FORK_TOKEN` **must** be configured as a
repository secret before the `publish-winget` CI job can submit manifest PRs to
`microsoft/winget-pkgs`.

**Why this is required:** `wingetcreate update --submit` forks `microsoft/winget-pkgs`
under the token owner's GitHub account, commits the updated manifests, and opens a PR.
This requires write access to the fork (which wingetcreate creates automatically) and
the ability to open PRs on the upstream repo. `GITHUB_TOKEN` does not have access to
external repos.

**Token type: classic PAT only.** wingetcreate does not support fine-grained tokens.
Fine-grained PATs will produce an authentication error. See
[wingetcreate token docs](https://github.com/microsoft/winget-create/blob/main/doc/token.md).

**Required scopes:**
- `public_repo` — mandatory; allows forking and PR creation on public repos
- `delete_repo` — optional; allows wingetcreate to clean up failed fork attempts

**Symptom of missing or expired token:** The `publish-winget` job completes with exit 0
but logs `::warning::wingetcreate exited with code <N>`. If the bootstrap PR has already
been merged (the package is in the index), a non-zero exit is most likely a token problem.
Check that the secret is set and the PAT has not expired.

---

## Creating the WINGET_FORK_TOKEN PAT

wingetcreate requires a **classic** PAT. Fine-grained tokens are explicitly not supported.

1. Navigate to https://github.com/settings/tokens (classic tokens page)
2. Click **Generate new token (classic)**.
3. Set a descriptive note (e.g., `nsyte winget publish`).
4. Set an expiry date — 90 days recommended. Avoid "No expiration".
5. Select scope: `public_repo` (under the `repo` group). Optionally also select
   `delete_repo` (under the top-level `delete_repo` checkbox).
6. Click **Generate token**. Copy the value immediately — it starts with `ghp_`.

---

## Adding WINGET_FORK_TOKEN to repository secrets

1. Navigate to https://github.com/sandwichfarm/nsyte/settings/secrets/actions
2. Click **New repository secret**.
3. Name: `WINGET_FORK_TOKEN` — exactly this, case-sensitive, no whitespace.
4. Value: paste the classic PAT from above.
5. Click **Add secret**.

Confirm the secret exists with the GitHub CLI:

```bash
gh secret list -R sandwichfarm/nsyte
```

`WINGET_FORK_TOKEN` should appear alongside the other publish secrets.

**Rotation:** Classic PATs expire. Set a calendar reminder before the expiry date.
Create a replacement PAT following the same steps, then update the secret value in
repository settings. The symptom of an expired token is the `publish-winget` job
exiting 0 with a warning after the bootstrap PR has been merged.

---

## Bootstrapping the winget package (one-time, WINGET-04)

The very first submission of `sandwichfarm.nsyte` to `microsoft/winget-pkgs` must be a
**manual PR**. This is Microsoft's preferred path for new packages — it allows human
review of publisher metadata, license, and installer type before automation takes over.
After the bootstrap PR is merged and the package appears in the index, all subsequent
version bumps are handled automatically by the `publish-winget` CI job.

### Bootstrap procedure

1. **Fork `microsoft/winget-pkgs`** at https://github.com/microsoft/winget-pkgs — click
   **Fork** and create the fork under the `sandwichfarm` account.

2. **Create the manifest directory in your fork:**
   ```
   manifests/s/sandwichfarm/nsyte/<VERSION>/
   ```
   where `<VERSION>` is the release version string (e.g., `1.6.0`).

3. **Copy and fill in the three YAML files** from `packages/winget/` in this repo:

   For each file, replace the PLACEHOLDER values:
   - `PLACEHOLDER_VERSION` → the real version (e.g., `1.6.0`)
   - `PLACEHOLDER_SHA256_WINDOWS` → the SHA256 of `nsyte-windows-<VERSION>.exe` from the
     GitHub release page

   ```bash
   VERSION="1.6.0"
   SHA256=$(curl -fsSL "https://github.com/sandwichfarm/nsyte/releases/download/v${VERSION}/nsyte-windows-${VERSION}.exe" | sha256sum | awk '{print $1}')

   mkdir -p /tmp/winget-bootstrap/manifests/s/sandwichfarm/nsyte/${VERSION}

   for f in packages/winget/sandwichfarm.nsyte.*.yaml; do
     dest="/tmp/winget-bootstrap/manifests/s/sandwichfarm/nsyte/${VERSION}/$(basename $f)"
     sed -e "s/PLACEHOLDER_VERSION/${VERSION}/g" \
         -e "s/PLACEHOLDER_SHA256_WINDOWS/${SHA256}/g" \
         "$f" > "$dest"
   done

   # Sanity: no PLACEHOLDER markers should remain
   grep -r "PLACEHOLDER_" /tmp/winget-bootstrap/ && { echo "substitution failed"; exit 1; }
   ```

4. **Commit the three YAML files** to a branch in your fork named
   `sandwichfarm.nsyte-<VERSION>` (convention; not enforced).

5. **Open a PR** from your fork branch to `microsoft/winget-pkgs` main.
   PR title convention: `New package: sandwichfarm.nsyte version <VERSION>`

6. **Wait for Microsoft review.** The winget-pkgs maintainers typically review new package
   PRs within 1–3 business days. Automated checks (validation bots) run first; manual
   review follows if validation passes.

7. **After the PR is merged**, the `publish-winget` CI job will work automatically on the
   next release — wingetcreate will find the package in the index and submit a version-bump
   PR.

**Note on the `publish-winget` job before bootstrap merge:** Until the bootstrap PR is
merged, the `publish-winget` CI job will exit 0 with a warning
(`wingetcreate exited with code <N>`). This is expected and by design — the job does not
fail the workflow while the package is not yet in the index.

---

## Verifying the winget manifests locally (WINGET-02)

A maintainer can generate the manifests locally without submitting to verify they are
well-formed. This requires Windows (or a Windows runner) and `wingetcreate`.

```powershell
# Pick a published version
$VERSION = "1.6.0"
$REPO = "sandwichfarm/nsyte"

# Download wingetcreate (self-contained, no .NET install needed)
iwr https://aka.ms/wingetcreate/latest/self-contained -OutFile $env:TEMP\wingetcreate.exe

# Generate manifests locally without submitting
# --out writes to a local directory; --submit is omitted
& "$env:TEMP\wingetcreate.exe" update sandwichfarm.nsyte `
  --version $VERSION `
  --urls "https://github.com/$REPO/releases/download/v$VERSION/nsyte-windows-$VERSION.exe|x64" `
  --out "$env:TEMP\winget-test"

# Inspect the generated manifests
Get-ChildItem "$env:TEMP\winget-test\manifests\s\sandwichfarm\nsyte\$VERSION\"
```

If wingetcreate produces three YAML files (version, installer, locale) in the output
directory with the correct version and a real SHA256 (which wingetcreate downloads and
computes from the installer URL), the manifests are well-formed and ready for submission.

**Note:** `wingetcreate update` without `--submit` requires the package to already exist
in the winget-pkgs index. Before the bootstrap PR is merged, use the manual substitution
steps in "Bootstrapping the winget package" above to produce and visually inspect the
initial YAML files instead.

---
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nsyte && grep -c "WINGET_FORK_TOKEN" docs/RELEASING.md && grep -c "Bootstrapping the winget package" docs/RELEASING.md && grep -c "Verifying the winget manifests locally" docs/RELEASING.md && grep -c "classic PAT" docs/RELEASING.md && python3 -c "
content = open('docs/RELEASING.md').read()
assert '## Related files' in content, 'Related files section missing'
winget_pos = content.index('## WINGET_FORK_TOKEN prerequisite')
related_pos = content.index('## Related files')
assert winget_pos < related_pos, 'WINGET sections must appear before Related files'
print('PASS: WINGET sections present and ordered correctly')
"
</automated>
  </verify>
  <done>
docs/RELEASING.md contains, in order before "## Related files":
- "## WINGET_FORK_TOKEN prerequisite" with classic PAT requirement and symptom description
- "## Creating the WINGET_FORK_TOKEN PAT" with step-by-step classic PAT creation
- "## Adding WINGET_FORK_TOKEN to repository secrets" with gh secret list verification
- "## Bootstrapping the winget package (one-time, WINGET-04)" with full bootstrap procedure
- "## Verifying the winget manifests locally (WINGET-02)" with --out dry-run approach
All existing RELEASING.md content is preserved unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → wingetcreate | Self-contained exe downloaded from Microsoft redirect URL; no integrity check |
| CI runner → microsoft/winget-pkgs | wingetcreate authenticates via WINGET_FORK_TOKEN PAT; token scoped to public_repo only |
| wingetcreate → installer URL | wingetcreate downloads the Windows exe to compute SHA256; URL is constructed from trusted GitHub release outputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | wingetcreate download (aka.ms redirect) | accept | Microsoft-hosted redirect; self-contained exe; risk equivalent to any CI tool bootstrap. Pinning a specific release hash would be the mitigation but is not standard practice for this tool. |
| T-25-02 | Information Disclosure | WINGET_FORK_TOKEN secret | mitigate | Token passed via `WINGET_CREATE_GITHUB_TOKEN` env var (the env var wingetcreate reads natively), never via `--token` flag which would appear in CI logs. All `${{ secrets.* }}` references are in `env:` blocks only. |
| T-25-03 | Elevation of Privilege | WINGET_FORK_TOKEN scope | mitigate | Classic PAT scoped to `public_repo` only — grants access to public repos solely. Does not grant write access to `sandwichfarm/nsyte` itself. `delete_repo` scope is optional and limited to fork cleanup. |
| T-25-04 | Denial of Service | winget-pkgs fork sync lag | accept | wingetcreate auto-creates/syncs the fork; if fork is severely behind, the job warns and exits 0. Documented in RELEASING.md. Risk is delayed (not blocked) publish. |
| T-25-05 | Spoofing | Installer URL construction | accept | URL is constructed from `github.repository` context and `setup` job version output — both are trusted GitHub Actions values, not user-controlled inputs in the release event path. |
</threat_model>

<verification>
After both tasks complete:

1. YAML syntax check on the modified workflow:
   ```bash
   python3 -c "import yaml; yaml.safe_load(open('.github/workflows/publish-packages.yml'))" && echo "YAML valid"
   ```

2. Confirm publish-winget is no longer the stub (ubuntu-latest → windows-latest):
   ```bash
   grep -A3 "publish-winget:" .github/workflows/publish-packages.yml | grep "runs-on"
   # Expected: runs-on: windows-latest
   ```

3. Confirm no ${{ }} in run blocks:
   ```bash
   python3 -c "
import yaml, re
wf = yaml.safe_load(open('.github/workflows/publish-packages.yml'))
job = wf['jobs']['publish-winget']
for step in job['steps']:
    run = step.get('run', '')
    assert not re.search(r'\\\${{', run), f'Found expression in run block of step: {step.get(\"name\")}'
print('PASS: no expressions in run blocks')
"
   ```

4. Confirm RELEASING.md section order:
   ```bash
   grep -n "## WINGET_FORK_TOKEN\|## Bootstrapping the winget\|## Verifying the winget\|## Related files" docs/RELEASING.md
   # WINGET sections must all appear before Related files
   ```
</verification>

<success_criteria>
- publish-winget job uses `runs-on: windows-latest`
- WINGET_CREATE_GITHUB_TOKEN appears in env: block of the submit step, not as a CLI flag
- --submit, --no-open, and |x64 arch override all present in the wingetcreate invocation
- Non-zero wingetcreate exit produces ::warning:: annotations and exits 0 (graceful pre-bootstrap)
- ManifestVersion informational check step present (resolves WR-02)
- RELEASING.md contains all five winget sections ordered before "## Related files"
- RELEASING.md explicitly states classic PAT is required (fine-grained not supported)
- Bootstrap procedure covers: fork, directory creation, PLACEHOLDER substitution, PR submission
- Local verification section uses --out flag (no --submit) for dry-run manifest inspection
- No existing content in publish-packages.yml or RELEASING.md is modified or removed
</success_criteria>

<output>
After completion, create `.planning/phases/25-winget-pipeline/25-01-SUMMARY.md` following
the template at `$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
