{
  "name": "nsyte-docs-site",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:build": "vitepress build docs && node scripts/prepare-vitepress-directory-urls.js",
    "docs:dev": "vitepress dev docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
