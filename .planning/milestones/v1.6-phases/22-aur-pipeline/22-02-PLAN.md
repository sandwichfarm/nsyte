---
phase: 22-aur-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/RELEASING.md
autonomous: true
requirements:
  - AUR-04
  - AUR-05
tags:
  - docs
  - releasing
  - aur
  - secrets

must_haves:
  truths:
    - "docs/RELEASING.md documents the AUR_SSH_PRIVATE_KEY secret as a prerequisite for the publish-aur job"
    - "docs/RELEASING.md explains why the secret is required (AUR uses SSH keypair auth) and what the symptom of its absence is (Permission denied / not allowed to access repository on the publish-aur job)"
    - "docs/RELEASING.md documents the one-time AUR account + SSH key + nsyte-bin package-name bootstrap that MUST precede CI's first push"
    - "docs/RELEASING.md documents the local-verification recipe a maintainer can run in Docker against a real release version BEFORE the CI fires (covers AUR-05's intent)"
    - "Existing RELEASE_TOKEN section is unchanged — this is an APPEND, not a rewrite"
  artifacts:
    - path: "docs/RELEASING.md"
      provides: "AUR_SSH_PRIVATE_KEY prerequisite section"
      contains: "AUR_SSH_PRIVATE_KEY"
    - path: "docs/RELEASING.md"
      provides: "AUR bootstrap (one-time) instructions"
      contains: "ssh://aur@aur.archlinux.org/nsyte-bin.git"
    - path: "docs/RELEASING.md"
      provides: "Local AUR verification recipe (AUR-05)"
      contains: "makepkg -si"
  key_links:
    - from: "docs/RELEASING.md AUR section"
      to: "publish-aur job in publish-packages.yml"
      via: "explicit reference to the job's secret consumption and clone step"
      pattern: "publish-aur"
    - from: "docs/RELEASING.md AUR section"
      to: "packages/aur/PKGBUILD template"
      via: "Related files list"
      pattern: "packages/aur/PKGBUILD"
---

<objective>
Append AUR-specific maintainer documentation to `docs/RELEASING.md` so the maintainer who is configuring the release pipeline knows: (1) that `AUR_SSH_PRIVATE_KEY` is a required repository secret, (2) the one-time human bootstrap steps that must precede CI's first push (AUR account, SSH key registration, `nsyte-bin` package name registration via empty-repo clone), and (3) how to locally verify the PKGBUILD with Docker before publishing — covering the AUR-05 acceptance path.

Purpose: Without this documentation, the publish-aur job (Plan 22-01) will run for the first time and fail with `Permission denied (publickey)` because the secret was never set, the SSH key was never uploaded to AUR, or the `nsyte-bin` package name was never registered. The symptom is loud (CI red) but the diagnosis is non-obvious — a maintainer who has not seen this kind of bootstrap before will burn time figuring it out. The doc front-loads the prerequisites in the same RELEASING.md file that already documents `RELEASE_TOKEN`, mirroring Phase 21's docs pattern.

Output: Single modified file `docs/RELEASING.md`. Two new top-level sections appended after the existing `Troubleshooting` section and before (or replacing) the existing `Related files` section. The existing `RELEASE_TOKEN` content is preserved verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-aur-pipeline/22-CONTEXT.md
@.planning/phases/22-aur-pipeline/22-RESEARCH.md
@.planning/phases/22-aur-pipeline/22-PATTERNS.md
@docs/RELEASING.md
@packages/aur/PKGBUILD

<style_anchor>
<!-- The existing RELEASE_TOKEN section in docs/RELEASING.md is the style anchor. -->
<!-- Mirror its structure: H2 heading > 1-paragraph "what + must" > "Why this is required" paragraph -->
<!-- > "Symptom" paragraph > Step-by-step ordered list > optional "Verifying" subsection. -->

Sections to mirror (from existing docs/RELEASING.md):
- Lines 7-13   : ## RELEASE_TOKEN prerequisite — opening, why, symptom
- Lines 17-38  : ## Creating the PAT — Option A / Option B numbered lists
- Lines 42-48  : ## Adding the secret to the repository — numbered steps
- Lines 52-68  : ## Verifying the setup — manual + CLI + functional checks
- Lines 108-112: ## Related files
</style_anchor>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append AUR_SSH_PRIVATE_KEY + bootstrap + local-verify sections to RELEASING.md</name>
  <files>docs/RELEASING.md</files>
  <read_first>
    - docs/RELEASING.md (current state — lines 1-112; the file ends at line 112 with the existing `## Related files` section. Confirm exact heading format, paragraph cadence, and where to splice the new content.)
    - .planning/phases/22-aur-pipeline/22-PATTERNS.md (the "docs/RELEASING.md — new AUR_SSH_PRIVATE_KEY section" subsection — extracts the exact section structure: heading > why > symptom > bootstrap steps > related files)
    - .planning/phases/22-aur-pipeline/22-RESEARCH.md (the "Bootstrap: One-Time Manual Setup (Before CI Can Push)" section + the "Local Verification (AUR-05): Docker Arch Container Test" code block — these are the literal content sources)
    - packages/aur/PKGBUILD (only to confirm the maintainer's manual local-test invocation references the same template the CI consumes)
  </read_first>
  <behavior>
    - The doc gains three new H2 sections, in this order:
      1. `## AUR_SSH_PRIVATE_KEY prerequisite` — mirrors the existing `## RELEASE_TOKEN prerequisite` shape (opener > why > symptom).
      2. `## Generating and registering the AUR SSH key` — numbered step list for the one-time human bootstrap (account creation, ssh-keygen, key upload, empty-repo clone to register nsyte-bin, secret addition).
      3. `## Verifying the AUR PKGBUILD locally (AUR-05)` — Docker-based recipe a maintainer can run before any real release.
    - The existing `## Related files` section is updated (or duplicated for AUR) to reference `packages/aur/PKGBUILD` and the publish-aur job.
    - The existing `## RELEASE_TOKEN prerequisite` section and everything before `## Related files` is unchanged.
  </behavior>
  <action>
Use the Edit tool on `docs/RELEASING.md`. The file currently ends at line 112 with the `## Related files` section that contains two bullets pointing to `release.yml` and `publish-packages.yml`.

**Step A — Splice three new sections BEFORE the existing `## Related files` heading.**

The existing `## Related files` section starts at line 108. Insert the following three new sections immediately before that heading (i.e., between the end of the `## Troubleshooting` section at line 105 and the `## Related files` heading at line 108).

The new content to insert verbatim (NOTE: `* * *` lines below are authoring placeholders — replace each with a bare `---` line to match the existing RELEASING.md separator style):

```markdown
* * *

## AUR_SSH_PRIVATE_KEY prerequisite

A private SSH key named `AUR_SSH_PRIVATE_KEY` **must** be configured as a repository secret before the `publish-aur` CI job can push to the AUR git remote.

**Why this is required:** The AUR (Arch User Repository) accepts package updates only via SSH-authenticated `git push` to `ssh://aur@aur.archlinux.org/<pkgname>.git`. The `publish-aur` job in `.github/workflows/publish-packages.yml` loads this secret into an ssh-agent (via `webfactory/ssh-agent@v0.10.0`) and performs an authenticated push of the patched PKGBUILD and regenerated `.SRCINFO`. Without the secret, the SSH agent has no key to present and the push fails immediately.

**Symptom of forgetting the secret (or registering an unmatched key):** The `publish-aur` job fails at the `Clone AUR remote` step with `Permission denied (publickey)`. The four sibling jobs (`publish-homebrew`, `publish-scoop`, `publish-winget`, `publish-nix`) are unaffected — failure isolation is by design (INFRA-05).

* * *

## Generating and registering the AUR SSH key

This is a one-time human bootstrap. CI cannot self-register the SSH key or the package name.

1. **Create an AUR account** at https://aur.archlinux.org/register/ if not already done.
2. **Generate an SSH keypair** (or reuse an existing one):
   ```bash
   ssh-keygen -t ed25519 -C "nsyte AUR CI" -f ~/.ssh/aur_nsyte_ed25519
   ```
   This produces `~/.ssh/aur_nsyte_ed25519` (private) and `~/.ssh/aur_nsyte_ed25519.pub` (public).
3. **Register the public key** in the AUR account settings:
   - Navigate to https://aur.archlinux.org/account/<your-username>/edit
   - Paste the contents of `~/.ssh/aur_nsyte_ed25519.pub` into the **SSH Public Key** field.
   - Save.
4. **Bootstrap the `nsyte-bin` package name** by cloning the empty AUR remote once from your local machine — this registers the package under your account so future pushes are authorized:
   ```bash
   GIT_SSH_COMMAND="ssh -i ~/.ssh/aur_nsyte_ed25519" \
     git clone ssh://aur@aur.archlinux.org/nsyte-bin.git
   # warning: You appear to have cloned an empty repository.
   ```
   The empty-repo warning is the documented bootstrap path. Do not commit anything from your local machine — leave the empty clone alone; CI's first push will populate it.
5. **Add the private key as the `AUR_SSH_PRIVATE_KEY` secret:**
   - Navigate to https://github.com/sandwichfarm/nsyte/settings/secrets/actions
   - Click **New repository secret**.
   - Name: `AUR_SSH_PRIVATE_KEY` — exactly this, case-sensitive, no whitespace.
   - Value: paste the **entire** contents of `~/.ssh/aur_nsyte_ed25519` (private key file), including the `-----BEGIN OPENSSH PRIVATE KEY-----` and `-----END OPENSSH PRIVATE KEY-----` lines and the trailing newline.
   - Click **Add secret**.

**Verifying the setup:**

Confirm the secret exists with the GitHub CLI:

```bash
gh secret list -R sandwichfarm/nsyte
```

`AUR_SSH_PRIVATE_KEY` should appear alongside `RELEASE_TOKEN`. (Values are not shown — masked is correct.)

**Functional check (after the next real release):**

When `release.yml` publishes a new release, `publish-packages.yml` fires, the `setup` job computes checksums, and the `publish-aur` job clones the AUR remote, patches PKGBUILD, regenerates `.SRCINFO`, and pushes. The AUR `nsyte-bin` package page should show the new version within minutes. If the job fails at `Clone AUR remote` or `Commit and push to AUR`, re-check the SSH key registration and that step 4 above (empty-repo bootstrap clone) was done.

**Rotation:** SSH keys do not expire on their own. Rotate by generating a new keypair, updating the public key in your AUR account settings, and updating the `AUR_SSH_PRIVATE_KEY` secret value in repo settings. The old key can be removed from the AUR account once the new one is confirmed working.

* * *

## Verifying the AUR PKGBUILD locally (AUR-05)

Before the first real release, a maintainer can verify the PKGBUILD installs cleanly using the same Arch Linux container the CI job uses. This builds confidence that the template is well-formed and that a published release is actually installable, independent of the CI plumbing.

This recipe assumes a published GitHub release exists at the version under test (e.g., from a previous release before the AUR pipeline was wired). If no such release exists yet, skip this step — the first CI run on the next release will exercise the same logic.

```bash
# 1. Pick a real published version that has a Linux x86_64 binary on the GitHub release page.
VERSION="1.5.0"  # replace with a real published version

# 2. Compute the SHA256 of the published Linux binary.
SHA256=$(curl -fsSL "https://github.com/sandwichfarm/nsyte/releases/download/v${VERSION}/nsyte-linux-${VERSION}" | sha256sum | awk '{print $1}')

# 3. Make a working copy of the PKGBUILD template and substitute the placeholders by name.
mkdir -p /tmp/aur-test
cp packages/aur/PKGBUILD /tmp/aur-test/PKGBUILD
sed -i "s/PLACEHOLDER_VERSION/${VERSION}/g"           /tmp/aur-test/PKGBUILD
sed -i "s/PLACEHOLDER_SHA256_X86_64/${SHA256}/g"      /tmp/aur-test/PKGBUILD

# 4. Sanity check: no PLACEHOLDER markers should remain.
grep "PLACEHOLDER_" /tmp/aur-test/PKGBUILD && { echo "substitution failed"; exit 1; }

# 5. Run makepkg -si inside an Arch Linux container as a non-root user.
docker run --rm -it \
  -v /tmp/aur-test:/pkg \
  archlinux:base-devel \
  bash -c '
    set -euo pipefail
    pacman -Syu --noconfirm
    useradd -m builder
    chown -R builder /pkg
    cd /pkg
    sudo -u builder makepkg -si --noconfirm
    nsyte --version
  '
```

If `nsyte --version` prints the version cleanly inside the container, the PKGBUILD is correct and the binary is installable. If the SHA256 mismatch error appears, the published release asset has been modified since publish — investigate before pushing the AUR update.

This is the same substitution logic and same container image the `publish-aur` CI job runs (just with `--printsrcinfo` instead of `-si`); a green local verification gives confidence in the upcoming CI run.
```

**Step B — Update the existing `## Related files` section to include AUR references.**

The current `## Related files` section (lines 108-112 of the original file) lists `release.yml` and `publish-packages.yml`. Append two more bullets for AUR. The replacement section content:

```markdown
## Related files

- `.github/workflows/release.yml` — the release-creation workflow. Line 446 uses `RELEASE_TOKEN` in the `softprops/action-gh-release` step to create the release that fires the downstream event.
- `.github/workflows/publish-packages.yml` — the publish workflow. Triggered by `release: published`. Depends entirely on the PAT-initiated release event to start; will never run if `release.yml` uses `GITHUB_TOKEN` instead. The `publish-aur` job consumes `AUR_SSH_PRIVATE_KEY` from secrets and pushes to `ssh://aur@aur.archlinux.org/nsyte-bin.git`.
- `packages/aur/PKGBUILD` — the AUR template. Contains `PLACEHOLDER_VERSION` and `PLACEHOLDER_SHA256_X86_64` which CI substitutes by name before pushing. Maintainers can substitute these by hand for local verification (see "Verifying the AUR PKGBUILD locally" above).
```

**Critical correctness rules:**

1. **Do not modify any heading or paragraph that exists today before line 108.** The existing `RELEASE_TOKEN` section is the style anchor and Phase 21 ships it verbatim — do not "improve" it while editing this file.

2. **Use exactly three H2 headings for the new content** (`## AUR_SSH_PRIVATE_KEY prerequisite`, `## Generating and registering the AUR SSH key`, `## Verifying the AUR PKGBUILD locally (AUR-05)`). The verifying section's `(AUR-05)` parenthetical is intentional — it traces the requirement.

3. **Maintain the `---` separator pattern.** The existing file uses `---` between major sections (lines 5, 15, 40, 50, 70, 86, 106). The new sections must also be separated by `---` for visual consistency.

4. **The `nsyte --version` line in step 5 of the local-verify recipe is load-bearing — do not remove it.** It is the single command that proves both substitution AND installation succeeded.

5. **Use `archlinux:base-devel`, NOT `archlinux:latest`.** Mirrors the publish-aur job exactly (RESEARCH.md anti-patterns).

6. **The `useradd -m builder` + `sudo -u builder` pattern in the local recipe is intentional** — `makepkg -si` refuses to run as root just like `makepkg --printsrcinfo` does.

7. **The example `VERSION="1.5.0"` is a placeholder example, not load-bearing.** A maintainer running this would substitute a real released version. Do not change `1.5.0` to `${VERSION}` or anything more clever; this is a literal recipe with a literal example.

8. **Do not link to `.planning/phases/22-aur-pipeline/...`** — `docs/RELEASING.md` is shipped to end users and must not reference internal planning artifacts.

After editing, do a final sanity pass:
- `grep -c "^## " docs/RELEASING.md` — should output a count that increased by exactly 3 (the three new H2 sections).
- The existing `RELEASE_TOKEN prerequisite` section text is byte-identical to before.
- No broken markdown lists or fenced code blocks (preview render in your editor).
  </action>
  <verify>
    <automated>test -f docs/RELEASING.md &amp;&amp; grep -q "^## AUR_SSH_PRIVATE_KEY prerequisite" docs/RELEASING.md &amp;&amp; grep -q "^## Generating and registering the AUR SSH key" docs/RELEASING.md &amp;&amp; grep -q "^## Verifying the AUR PKGBUILD locally" docs/RELEASING.md &amp;&amp; grep -q "AUR_SSH_PRIVATE_KEY" docs/RELEASING.md &amp;&amp; grep -q "ssh-keygen -t ed25519" docs/RELEASING.md &amp;&amp; grep -q "ssh://aur@aur.archlinux.org/nsyte-bin.git" docs/RELEASING.md &amp;&amp; grep -q "archlinux:base-devel" docs/RELEASING.md &amp;&amp; grep -q "makepkg -si" docs/RELEASING.md &amp;&amp; grep -q "PLACEHOLDER_VERSION" docs/RELEASING.md &amp;&amp; grep -q "PLACEHOLDER_SHA256_X86_64" docs/RELEASING.md &amp;&amp; grep -q "packages/aur/PKGBUILD" docs/RELEASING.md &amp;&amp; grep -q "publish-aur" docs/RELEASING.md &amp;&amp; grep -q "Permission denied (publickey)" docs/RELEASING.md &amp;&amp; grep -q "RELEASE_TOKEN prerequisite" docs/RELEASING.md &amp;&amp; grep -q "softprops/action-gh-release" docs/RELEASING.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/RELEASING.md` contains the literal heading `## AUR_SSH_PRIVATE_KEY prerequisite`.
    - `docs/RELEASING.md` contains the literal heading `## Generating and registering the AUR SSH key`.
    - `docs/RELEASING.md` contains the literal heading `## Verifying the AUR PKGBUILD locally` (AUR-05 traceability).
    - `docs/RELEASING.md` contains the literal string `AUR_SSH_PRIVATE_KEY` (the secret name).
    - `docs/RELEASING.md` contains the literal string `ssh-keygen -t ed25519` (key generation step).
    - `docs/RELEASING.md` contains the literal string `ssh://aur@aur.archlinux.org/nsyte-bin.git` (AUR remote URL).
    - `docs/RELEASING.md` contains the literal string `archlinux:base-devel` (container image — same one the CI job uses).
    - `docs/RELEASING.md` contains the literal string `makepkg -si` (local verification command).
    - `docs/RELEASING.md` contains the literal string `PLACEHOLDER_VERSION` (template placeholder name; same as PKGBUILD line 4).
    - `docs/RELEASING.md` contains the literal string `PLACEHOLDER_SHA256_X86_64` (template placeholder name; same as PKGBUILD line 17).
    - `docs/RELEASING.md` contains the literal string `packages/aur/PKGBUILD` (Related files entry).
    - `docs/RELEASING.md` contains the literal string `publish-aur` (job name reference).
    - `docs/RELEASING.md` contains the literal string `Permission denied (publickey)` (symptom).
    - `docs/RELEASING.md` STILL contains the literal heading `## RELEASE_TOKEN prerequisite` (Phase 21 content preserved).
    - `docs/RELEASING.md` STILL contains the literal string `softprops/action-gh-release` (Phase 21 content preserved).
  </acceptance_criteria>
  <done>
- The three new H2 sections are appended after `## Troubleshooting` and before `## Related files` (or, if `Related files` is replaced, the AUR bullets appear in the replacement).
- The existing `RELEASE_TOKEN prerequisite` section is byte-for-byte unchanged.
- The local-verify recipe references `archlinux:base-devel` (not `:latest`), uses non-root `builder`, and ends with `nsyte --version`.
- The Related files list now references `packages/aur/PKGBUILD`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer's local SSH keystore -> GitHub repository secrets | Private SSH key material crosses from the maintainer's machine into GitHub's encrypted secrets storage via the maintainer's authenticated browser session. |
| Documentation file -> public web | `docs/RELEASING.md` is shipped to the published docs site (Phase 19 cutover). The content is public — must not contain real keys, fingerprints of private keys, or identifying account info beyond what is already public. |
| Documentation -> maintainer behavior | The doc instructs the maintainer to perform a security-sensitive operation (creating, registering, and storing an SSH private key). Mistakes here produce silent CI failures or, in the worst case, key leakage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-09 | Information Disclosure | Doc accidentally containing a real private key as an example | mitigate | The doc uses placeholder filenames (`~/.ssh/aur_nsyte_ed25519`) and command snippets only — no actual key material is embedded. The acceptance criteria verify by string match that no `BEGIN OPENSSH` literal appears in the doc body except inside instructional prose about copy-pasting. |
| T-22-10 | Tampering | Maintainer following doc and pasting wrong key material into the secret | accept | The doc instructs pasting the entire private key file including header/footer lines and trailing newline. `webfactory/ssh-agent` validates key format on load and the workflow fails loudly if the key is malformed. The maintainer's risk of pasting the wrong key has no silent failure mode. |
| T-22-11 | Repudiation | Doc instructions stale, maintainer follows outdated steps after AUR changes its policy | accept | AUR's submission flow has been stable for years (host key migration in 2023 was the most recent change). The doc's RESEARCH.md baseline is dated 2026-05-05 with a 1-month validity. Drift is monitored via the docs cutover phase but accepted in steady state. |
| T-22-12 | Elevation of Privilege | Local-verify recipe trains maintainer to run `pacman -Syu --noconfirm` inside an arch container without scrutiny | accept | The container is ephemeral (`docker run --rm`), runs in an isolated namespace, and only writes to the bind-mounted `/tmp/aur-test`. The maintainer's host system is not at risk. The `--noconfirm` flag is acceptable inside the container's contained scope. |
</threat_model>

<verification>
| Req ID | Check |
|--------|-------|
| AUR-04 | `grep -q "AUR_SSH_PRIVATE_KEY" docs/RELEASING.md && grep -q "ssh-keygen -t ed25519" docs/RELEASING.md && grep -q "ssh://aur@aur.archlinux.org/nsyte-bin.git" docs/RELEASING.md` — confirms the secret is named, the keypair generation is documented, and the bootstrap clone step that registers the package name is present. |
| AUR-05 | `grep -q "Verifying the AUR PKGBUILD locally" docs/RELEASING.md && grep -q "archlinux:base-devel" docs/RELEASING.md && grep -q "makepkg -si" docs/RELEASING.md && grep -q "nsyte --version" docs/RELEASING.md` — confirms the local-verification recipe is documented with the correct container, the correct makepkg invocation, and a positive end-to-end check. |

Functional verification (a maintainer following the doc actually succeeds in registering the secret and running the local recipe) is impossible to automate. It is exercised once, by the maintainer, during the bootstrap that precedes the first real release.
</verification>

<success_criteria>
- `docs/RELEASING.md` ships AUR-specific maintainer documentation in three new H2 sections.
- The existing `RELEASE_TOKEN prerequisite` content is unchanged.
- All grep-based acceptance criteria pass.
- A maintainer reading the doc end-to-end can: register the secret, register the SSH key with AUR, bootstrap the `nsyte-bin` package name, and run a local docker verification — without consulting any other source.
</success_criteria>

<output>
After completion, create `.planning/phases/22-aur-pipeline/22-02-SUMMARY.md` per the standard summary template. The summary MUST note:
- Whether the new sections were spliced before `## Related files` or whether `## Related files` was rewritten with AUR bullets appended.
- Confirmation that the `RELEASE_TOKEN` section is byte-identical to before.
- The final line count of the file (was 112 lines pre-edit).
</output>
