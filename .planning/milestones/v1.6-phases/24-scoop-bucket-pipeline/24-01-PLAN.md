---
id: 24-01
phase: 24-scoop-bucket-pipeline
plan: 01
title: "Replace publish-scoop stub with real Scoop bucket publish pipeline"
type: execute
wave: 1
status: pending
depends_on: []
autonomous: true
goal: >
  The publish-scoop CI job clones sandwichfarm/scoop-nsyte, patches
  packages/scoop/bucket/nsyte.json with version and SHA256 from the setup job,
  validates JSON, commits, and pushes via SCOOP_BUCKET_TOKEN PAT.
  docs/RELEASING.md documents the secret setup and bootstrap steps.
requirements: [SCOOP-02, SCOOP-03, SCOOP-04]
files_modified:
  - .github/workflows/publish-packages.yml
  - docs/RELEASING.md

must_haves:
  truths:
    - "publish-scoop job replaces the TODO stub with real clone-edit-push steps"
    - "PLACEHOLDER_VERSION is replaced in both the version field and the url field (global flag)"
    - "PLACEHOLDER_SHA256_WINDOWS is replaced in the hash field"
    - "grep PLACEHOLDER_ fail-fast check runs after substitution"
    - "jq empty JSON validation runs after substitution"
    - "git diff --cached --quiet idempotency check skips push if manifest is already up to date"
    - "RELEASING.md documents SCOOP_BUCKET_TOKEN PAT creation, secret registration, repo bootstrap, and local verification recipe"
  artifacts:
    - path: ".github/workflows/publish-packages.yml"
      provides: "Real publish-scoop job"
      contains: "sandwichfarm/scoop-nsyte"
    - path: "docs/RELEASING.md"
      provides: "SCOOP_BUCKET_TOKEN setup and SCOOP-04 local verification recipe"
      contains: "SCOOP_BUCKET_TOKEN prerequisite"
  key_links:
    - from: "publish-scoop job"
      to: "needs.setup.outputs.sha256_windows"
      via: "SHA256_WINDOWS env var"
      pattern: "sha256_windows"
    - from: "Patch manifest step"
      to: "/tmp/scoop/bucket/nsyte.json"
      via: "sed global substitution"
      pattern: "PLACEHOLDER_VERSION.*g"
---

<objective>
Replace the TODO stub in the publish-scoop job with the real clone-patch-push pipeline.
Mirror the pattern established by publish-homebrew: clone the bucket repo without
credentials, set an authenticated remote for push only, copy the template manifest,
substitute both placeholders (PLACEHOLDER_VERSION appears twice — in version and url —
so the global /g flag is required), run a placeholder sanity check and jq JSON
validation, then commit and push if the manifest changed.

Also add SCOOP_BUCKET_TOKEN prerequisite, PAT creation, secret registration, one-time
bucket bootstrap, and local verification (SCOOP-04) sections to docs/RELEASING.md,
parallel to the existing HOMEBREW_TAP_TOKEN sections.

Purpose: Completes the Scoop distribution channel so Windows users receive updated
manifests automatically on every release without manual intervention.
Output: Updated publish-packages.yml, updated RELEASING.md.
</objective>

<execution_context>
@/home/sandwich/.claude/get-shit-done/workflows/execute-plan.md
@/home/sandwich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.github/workflows/publish-packages.yml
@packages/scoop/bucket/nsyte.json
@docs/RELEASING.md
</context>

<interfaces>
<!-- Key facts extracted from codebase for executor. No exploration needed. -->

From .github/workflows/publish-packages.yml:
- publish-scoop stub is at lines 223-237 (single TODO step)
- publish-homebrew job structure (lines 172-221) is the authoritative pattern to mirror
- setup job outputs available: version, sha256_windows (line 30)
- publish-homebrew uses: checkout@v4, clone without credentials, set-url with token, mkdir -p, cp, sed, grep PLACEHOLDER check, git config, git add, diff --cached, commit, push origin main

From packages/scoop/bucket/nsyte.json:
- PLACEHOLDER_VERSION appears on line 2 (version field) and line 6 (url field) — requires /g flag
- PLACEHOLDER_SHA256_WINDOWS appears on line 7 (hash field) — one occurrence
- autoupdate block (lines 12-18) uses $version — no placeholders, no substitution needed

From docs/RELEASING.md:
- HOMEBREW_TAP_TOKEN sections span lines 207-325 (prerequisite, PAT creation, secret registration, bootstrap, local verification)
- "## Related files" section starts at line 329 — insert Scoop sections before this line
- Related files section references packages/homebrew/Formula/nsyte.rb — will need nsyte.json added
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace publish-scoop stub in publish-packages.yml</name>
  <files>.github/workflows/publish-packages.yml</files>
  <action>
Replace the entire publish-scoop job (lines 223-237) with the real implementation.
The new job structure mirrors publish-homebrew exactly with Scoop-specific values.

Replace this block:
```yaml
  publish-scoop:
    name: Publish to Scoop
    needs: [setup]
    if: needs.setup.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: TODO — phase 24 will replace this stub
        env:
          VERSION: ${{ needs.setup.outputs.version }}
        run: |
          echo "TODO: phase 24 will implement Scoop publish here."
          echo "Version available: ${VERSION}"
          echo "Stub exiting 0 — workflow scaffold validation only."
```

With this real implementation:
```yaml
  publish-scoop:
    name: Publish to Scoop
    needs: [setup]
    if: needs.setup.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout nsyte repo (for manifest template)
        uses: actions/checkout@v4

      - name: Clone Scoop bucket repo
        env:
          SCOOP_BUCKET_TOKEN: ${{ secrets.SCOOP_BUCKET_TOKEN }}
        run: |
          git clone "https://github.com/sandwichfarm/scoop-nsyte.git" /tmp/scoop
          git -C /tmp/scoop remote set-url origin "https://x-access-token:${SCOOP_BUCKET_TOKEN}@github.com/sandwichfarm/scoop-nsyte.git"

      - name: Patch manifest from template
        env:
          VERSION: ${{ needs.setup.outputs.version }}
          SHA256_WINDOWS: ${{ needs.setup.outputs.sha256_windows }}
        run: |
          mkdir -p /tmp/scoop/bucket
          cp packages/scoop/bucket/nsyte.json /tmp/scoop/bucket/nsyte.json
          sed -i "s|PLACEHOLDER_VERSION|${VERSION}|g"               /tmp/scoop/bucket/nsyte.json
          sed -i "s|PLACEHOLDER_SHA256_WINDOWS|${SHA256_WINDOWS}|g" /tmp/scoop/bucket/nsyte.json
          # Sanity: fail fast if any placeholder survived substitution
          if grep -q "PLACEHOLDER_" /tmp/scoop/bucket/nsyte.json; then
            echo "::error::Manifest still contains PLACEHOLDER_ markers after substitution."
            grep "PLACEHOLDER_" /tmp/scoop/bucket/nsyte.json
            exit 1
          fi
          # JSON validation
          jq empty /tmp/scoop/bucket/nsyte.json

      - name: Commit and push to bucket repo
        env:
          VERSION: ${{ needs.setup.outputs.version }}
        run: |
          git -C /tmp/scoop config user.name "nsyte-bot"
          git -C /tmp/scoop config user.email "nsyte-bot@users.noreply.github.com"
          git -C /tmp/scoop add bucket/nsyte.json
          if git -C /tmp/scoop diff --cached --quiet; then
            echo "No changes to bucket/nsyte.json — bucket already up to date for v${VERSION}. Skipping push."
            exit 0
          fi
          git -C /tmp/scoop commit -m "nsyte ${VERSION}"
          git -C /tmp/scoop push origin main
```

Security notes:
- No ${{ }} expressions inside run: blocks (env: block passes secrets safely)
- Clone uses plain HTTPS (no credentials); authenticated remote set separately for push only
- This matches the pattern in publish-homebrew exactly
  </action>
  <verify>
    <automated>grep -q "sandwichfarm/scoop-nsyte" /home/sandwich/Develop/nsyte/.github/workflows/publish-packages.yml && grep -q "SCOOP_BUCKET_TOKEN" /home/sandwich/Develop/nsyte/.github/workflows/publish-packages.yml && grep -q "jq empty" /home/sandwich/Develop/nsyte/.github/workflows/publish-packages.yml && grep -q "PLACEHOLDER_VERSION.*g" /home/sandwich/Develop/nsyte/.github/workflows/publish-packages.yml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    publish-scoop job contains four steps: checkout, clone-bucket-repo, patch-manifest, commit-and-push.
    The sed PLACEHOLDER_VERSION substitution uses the /g flag.
    PLACEHOLDER_ sanity check and jq empty validation are present.
    git diff --cached --quiet idempotency guard is present.
    No ${{ }} expressions appear inside any run: block (secrets passed via env: only).
    Stub TODO step is gone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append Scoop sections to docs/RELEASING.md</name>
  <files>docs/RELEASING.md</files>
  <action>
Insert five new sections into docs/RELEASING.md immediately before the existing
"## Related files" section (currently the last section). Also update the "## Related files"
bullet list to reference the Scoop manifest template.

Insert this block before "## Related files":

```markdown
---

## SCOOP_BUCKET_TOKEN prerequisite

A Personal Access Token (PAT) named `SCOOP_BUCKET_TOKEN` **must** be configured as a
repository secret before the `publish-scoop` CI job can push to the Scoop bucket repo.

**Why this is required:** The Scoop bucket (`sandwichfarm/scoop-nsyte`) is a separate
GitHub repository. The `publish-scoop` job clones it, patches `bucket/nsyte.json` with
the release version and SHA256 hash, and pushes the commit back. `GITHUB_TOKEN` only has
write access to the repo it runs in (`sandwichfarm/nsyte`), not to the separate bucket
repo. A PAT with Contents write permission on `sandwichfarm/scoop-nsyte` is required.

**Symptom of forgetting the secret:** The `publish-scoop` job fails at the clone or
push step with `Authentication failed` or `repository not found`. The sibling jobs
(`publish-aur`, `publish-homebrew`, etc.) are unaffected — failure isolation is by
design (INFRA-05).

---

## Creating the SCOOP_BUCKET_TOKEN PAT (fine-grained, preferred)

1. Navigate to https://github.com/settings/personal-access-tokens
2. Click **Generate new token**.
3. Set a descriptive name (e.g., `nsyte scoop bucket publish`).
4. Set an expiry date (90 days recommended). Avoid "No expiration".
5. Under **Repository access**, select **Only select repositories** → choose
   `sandwichfarm/scoop-nsyte`. Do **not** grant access to `sandwichfarm/nsyte` — that
   would violate least privilege.
6. Under **Repository permissions**, set **Contents** to **Read and write**. No other
   permissions are needed.
7. Click **Generate token**. Copy immediately — it starts with `github_pat_`.

---

## Adding SCOOP_BUCKET_TOKEN to repository secrets

1. Navigate to https://github.com/sandwichfarm/nsyte/settings/secrets/actions
2. Click **New repository secret**.
3. Name: `SCOOP_BUCKET_TOKEN` — exactly this, case-sensitive, no whitespace.
4. Value: paste the PAT string from above.
5. Click **Add secret**.

Confirm the secret exists with the GitHub CLI:

```bash
gh secret list -R sandwichfarm/nsyte
```

`SCOOP_BUCKET_TOKEN` should appear alongside `HOMEBREW_TAP_TOKEN`, `AUR_SSH_PRIVATE_KEY`,
and `RELEASE_TOKEN`.

---

## Bootstrapping the Scoop bucket (one-time)

The `sandwichfarm/scoop-nsyte` repository must exist before CI can push to it. This is a
one-time manual step.

1. **Create the bucket repo** — navigate to https://github.com/new and create a **public**
   repo named `scoop-nsyte` under the `sandwichfarm` account. Unlike Homebrew, Scoop bucket
   repos do **not** require a `scoop-` prefix — the repo name is arbitrary; the bucket name
   that users register is what matters (`nsyte`). Initialize with a `README.md` so the
   default branch (`main`) exists.
2. **Ensure the default branch is `main`** — GitHub's current default is `main`; if the
   repo was created with `master`, rename it before the first CI push or update the push
   target in the workflow.
3. **Add `SCOOP_BUCKET_TOKEN` secret** — follow the steps above.
4. **Trigger a release** — the `publish-scoop` job will clone the repo, create
   `bucket/nsyte.json`, commit, and push. The first CI push populates the `bucket/`
   directory.

**End-user install command (after first CI push):**
```bash
scoop bucket add nsyte https://github.com/sandwichfarm/scoop-nsyte
scoop install nsyte
```

**Empty-repo note:** `git clone` of a repo that contains only `README.md` succeeds.
The `mkdir -p /tmp/scoop/bucket` in the patch step ensures `bucket/` is created even if
it does not exist in the cloned bucket repo yet.

**If the bucket repo does not exist at clone time:** The clone step fails with
`repository not found`. Create the repo first, then re-run the workflow via
`workflow_dispatch`.

---

## Verifying the Scoop manifest locally (SCOOP-04)

A maintainer can verify the patched manifest is well-formed before a real CI run.
This requires `jq` and `curl` (standard on Linux maintainer machines).

```bash
# Pick a real published version that has a Windows .exe on the GitHub release page
VERSION="1.5.0"  # replace with a real published version

# Compute SHA256 of the published Windows binary
SHA256_WINDOWS=$(curl -fsSL "https://github.com/sandwichfarm/nsyte/releases/download/v${VERSION}/nsyte-windows-${VERSION}.exe" | sha256sum | awk '{print $1}')

# Patch a working copy (same logic as CI)
mkdir -p /tmp/scoop-test/bucket
cp packages/scoop/bucket/nsyte.json /tmp/scoop-test/bucket/nsyte.json
sed -i "s|PLACEHOLDER_VERSION|${VERSION}|g"               /tmp/scoop-test/bucket/nsyte.json
sed -i "s|PLACEHOLDER_SHA256_WINDOWS|${SHA256_WINDOWS}|g" /tmp/scoop-test/bucket/nsyte.json

# Sanity check: no PLACEHOLDER markers should remain
grep "PLACEHOLDER_" /tmp/scoop-test/bucket/nsyte.json && { echo "substitution failed"; exit 1; }

# JSON validation
jq empty /tmp/scoop-test/bucket/nsyte.json && echo "JSON is valid"

# Pretty-print for visual inspection
jq . /tmp/scoop-test/bucket/nsyte.json
```

If `jq .` prints the manifest cleanly with no PLACEHOLDER strings and the `url` and `hash`
fields contain real values, the manifest is correct. Note that `PLACEHOLDER_VERSION` is
embedded in the `url` field as well as the `version` field — the `sed /g` flag in the CI
step (and in the recipe above) replaces both occurrences in a single pass.

**End-to-end functional check (after a real CI push to the bucket):**

```bash
scoop bucket add nsyte https://github.com/sandwichfarm/scoop-nsyte
scoop install nsyte
nsyte --version
```

This is the end-user install path and the final functional check after a real release.

---

```

After inserting the block above, update the "## Related files" bullet list to add
a line for the Scoop template. Append after the existing homebrew formula bullet:

```
- `packages/scoop/bucket/nsyte.json` — the Scoop manifest template. Contains `PLACEHOLDER_VERSION` (appears in both the `version` field and the `url` field) and `PLACEHOLDER_SHA256_WINDOWS`, which CI substitutes per-release before pushing to the bucket. Maintainers can substitute these by hand for local verification (see "Verifying the Scoop manifest locally" above).
```
  </action>
  <verify>
    <automated>grep -q "SCOOP_BUCKET_TOKEN prerequisite" /home/sandwich/Develop/nsyte/docs/RELEASING.md && grep -q "Verifying the Scoop manifest locally" /home/sandwich/Develop/nsyte/docs/RELEASING.md && grep -q "scoop-nsyte" /home/sandwich/Develop/nsyte/docs/RELEASING.md && grep -q "packages/scoop/bucket/nsyte.json" /home/sandwich/Develop/nsyte/docs/RELEASING.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    docs/RELEASING.md contains five new sections before "## Related files":
    SCOOP_BUCKET_TOKEN prerequisite, Creating the PAT, Adding to repository secrets,
    Bootstrapping the bucket (with scoop bucket add command and note about no required prefix),
    and Verifying locally (SCOOP-04) with the sed + jq verification recipe.
    The "## Related files" section includes a bullet for packages/scoop/bucket/nsyte.json.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → scoop-nsyte bucket repo | Authenticated push via SCOOP_BUCKET_TOKEN PAT in remote URL |
| setup job outputs → publish-scoop env vars | VERSION and SHA256_WINDOWS flow through needs.setup.outputs — values originate from release assets computed by the setup job |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | sed substitution in Patch manifest step | mitigate | grep PLACEHOLDER_ sanity check + jq empty validation run after substitution; job exits 1 if either check fails |
| T-24-02 | Information Disclosure | SCOOP_BUCKET_TOKEN in run: block | mitigate | Token passed via env: block only, never interpolated as ${{ }} inside run: shell; GitHub masks env vars in logs |
| T-24-03 | Elevation of Privilege | PAT scope on scoop-nsyte repo | mitigate | Fine-grained PAT with Contents: Read and write scoped only to sandwichfarm/scoop-nsyte (documented in RELEASING.md) |
| T-24-04 | Denial of Service | Push to wrong branch (master vs main) | accept | RELEASING.md bootstrap steps instruct maintainer to ensure default branch is main; low risk, easily corrected via workflow_dispatch re-run |
| T-24-05 | Tampering | Malformed JSON pushed to bucket | mitigate | jq empty exits non-zero on invalid JSON; job fails before git commit |
</threat_model>

<verification>
After both tasks are complete:

1. Workflow structure check — the publish-scoop job must have exactly four named steps (checkout, clone bucket repo, patch manifest, commit and push) with no TODO text remaining.

2. Placeholder flag check — verify PLACEHOLDER_VERSION sed command uses `|g` not just `|`:
   `grep "PLACEHOLDER_VERSION" .github/workflows/publish-packages.yml`
   Both the sed line and the grep sanity line should appear.

3. RELEASING.md section order check — sections must appear in this order:
   SCOOP_BUCKET_TOKEN prerequisite → Creating the PAT → Adding to secrets → Bootstrap → Local verification → Related files.

4. Security check — no `${{ secrets.SCOOP_BUCKET_TOKEN }}` expression inside any `run:` block:
   `grep -n 'secrets\.' .github/workflows/publish-packages.yml`
   All secret references must be in `env:` blocks only.

5. JSON validation step present:
   `grep "jq empty" .github/workflows/publish-packages.yml`
   Must return a match.
</verification>

<success_criteria>
- publish-scoop job in publish-packages.yml has no TODO stub; real pipeline is present
- PLACEHOLDER_VERSION is substituted with /g flag (covers both version and url fields)
- PLACEHOLDER_SHA256_WINDOWS is substituted correctly
- Placeholder sanity check (grep -q PLACEHOLDER_) present and will fail the job on miss
- jq empty JSON validation present
- Idempotency guard (git diff --cached --quiet) present — skips push if manifest unchanged
- No ${{ }} expressions inside run: blocks; all secrets via env: only
- RELEASING.md documents SCOOP_BUCKET_TOKEN: why required, how to create PAT, how to register secret, one-time bootstrap steps (including scoop bucket add command and no-prefix note), and SCOOP-04 local verification recipe
- RELEASING.md "## Related files" references packages/scoop/bucket/nsyte.json
</success_criteria>

<output>
After completion, create `.planning/phases/24-scoop-bucket-pipeline/24-01-SUMMARY.md`
using the summary template at `@/home/sandwich/.claude/get-shit-done/templates/summary.md`.
</output>
