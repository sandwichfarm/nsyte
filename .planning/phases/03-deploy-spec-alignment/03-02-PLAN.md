# Plan 02: NIP-5A Specification Document Alignment

---
wave: 1
depends_on: []
files_modified:
  - nsite-nip.md (deleted)
  - NIP-5A.md (created)
requirements: [DOC-01]
autonomous: true
---

<objective>
Replace the outdated `nsite-nip.md` draft specification with a minimal `NIP-5A.md` that links to the published NIP-5A specification, eliminating duplicated/outdated spec content from the repository.
</objective>

## Tasks

<task id="1">
<title>Delete nsite-nip.md and create NIP-5A.md</title>
<read_first>
- nsite-nip.md (current content — verify it exists and understand what's being replaced)
</read_first>
<action>
1. Delete `nsite-nip.md` from the repository root:
```bash
git rm nsite-nip.md
```

2. Create `NIP-5A.md` at the repository root with the following content:
```markdown
# NIP-5A: Pubkey Static Websites

nsyte implements the [NIP-5A specification](https://github.com/nostr-protocol/nips/blob/master/5A.md) for hosting static websites on nostr using public keys.

See the [published specification](https://github.com/nostr-protocol/nips/blob/master/5A.md) for the full protocol definition.
```

This is intentionally minimal — the published NIP is the authoritative source. No spec content should be duplicated in the repo.
</action>
<acceptance_criteria>
- `nsite-nip.md` does not exist at repository root
- `NIP-5A.md` exists at repository root
- `NIP-5A.md` contains `https://github.com/nostr-protocol/nips/blob/master/5A.md`
- `NIP-5A.md` contains `NIP-5A` in the heading
- `NIP-5A.md` is less than 10 lines (minimal, no duplicated spec content)
- `NIP-5A.md` contains the word `nsyte`
</acceptance_criteria>
</task>

<task id="2">
<title>Update references to nsite-nip.md in codebase</title>
<read_first>
- Search all files for references to `nsite-nip.md` or `nsite-nip` to find any imports, links, or documentation references
</read_first>
<action>
Search the codebase for any references to `nsite-nip.md`:
```bash
grep -r "nsite-nip" --include="*.ts" --include="*.md" --include="*.json" .
```

For each reference found:
- If it's a file path reference (e.g., in a read_first or documentation): update to `NIP-5A.md`
- If it's a comment mentioning the old spec: update to reference "NIP-5A" or the published spec URL

Known references to check:
- `.planning/phases/01-encoding-validation-primitives/01-PLAN-01.md` task 1 read_first mentions `nsite-nip.md`
- Any README.md references
- Any source code comments

Note: `.planning/` files are historical artifacts and may not need updating, but source code and user-facing docs must be corrected.
</action>
<acceptance_criteria>
- `grep -r "nsite-nip" --include="*.ts" --include="*.json" .` returns no matches (no source code references remain)
- If README.md references nsite-nip.md, it is updated to NIP-5A.md
</acceptance_criteria>
</task>

## Verification

<verification>
All acceptance criteria verified by:
1. `test ! -f nsite-nip.md` exits 0 (old file deleted)
2. `test -f NIP-5A.md` exits 0 (new file exists)
3. `grep "nostr-protocol/nips/blob/master/5A.md" NIP-5A.md` finds the spec URL
4. `wc -l NIP-5A.md` shows less than 10 lines
5. `grep -r "nsite-nip" --include="*.ts" --include="*.json" .` returns empty (no stale references in code)
</verification>

## Must-Haves (Goal-Backward)

<must_haves>
Phase goal: "...and the internal spec doc matches the published NIP-5A"

1. nsite-nip.md is removed from the repository
2. NIP-5A.md exists and links to the published NIP-5A specification
3. NIP-5A.md does not duplicate spec content (link-only approach)
4. No stale references to nsite-nip.md remain in source code
</must_haves>
